const
    { RichEmbed } = require('discord.js'),
    config = require('../config.json')
module.exports = {
    run: async (client,message,args) => {

  },
    cmd: {
      name: " ",
      aliases: [" "],
      category: " ",
      desc: "",
      usage: " "
    }
}
